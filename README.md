# Resolv Recovery Console

Static, self-contained client-side console for recovering and equally splitting the
Resolv v2 governance treasury on Base via the protocol's own 3-of-5 resolvr vote.
No backend, no build step, no dependencies — every chain read/write goes through
your own browser wallet (MetaMask). Open the site, connect on Base, follow the plan.
